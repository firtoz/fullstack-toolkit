---
"@firtoz/idb-collections": patch
---

Align `keyvalCollectionOptions` return type with TanStack DB `CollectionConfig`’s fourth generic (`utils`), so TypeScript 7 native (`tsgo`) typechecks match `tsc` 6.
