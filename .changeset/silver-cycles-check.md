---
"@firtoz/websocket-do": minor
---

Add `@firtoz/websocket-do/ws-rpc-protocol` with `defineRpcProcedures`, `createRpcPromiseCall`, `rejectPendingRpcById`, and `RpcSessionLike` for library-owned RPC correlation over Zod WebSocket sessions.
