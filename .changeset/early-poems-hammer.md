---
"@firtoz/hono-fetcher": minor
---

Add optional `query` record on fetcher requests (alongside `params` and `init`) to append URL search parameters. `null` and `undefined` values are omitted. Export `HonoFetcherQueryParams` and `HonoFetcherQueryParamValue`.
