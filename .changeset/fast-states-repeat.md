---
"@firtoz/collection-sync": minor
---

Add `@firtoz/collection-sync/react` subpath with generic `usePartialSyncWindow` (TanStack-friendly `PartialSyncCollection`, `keyof`-typed sort columns, internal `PartialSyncClientBridge` + WebSocket via `connectPartialSync`), `usePredicateFilteredRows`, and shared helpers (`tryIdsForIndexWindow`, `computeFingerprintForIndexWindow`, `matchesPredicate`, `assertSyncUtils`). Requires `react` as a peer dependency.
