---
"@firtoz/websocket-do": minor
---

Add `@firtoz/websocket-do/rpc` (`ZodWebSocketRpcSession`, `createZodWebSocketRpcSession`) for pending-map WebSocket RPC over Zod-validated messages, and `@firtoz/websocket-do/rpc-react` with `useZodWebSocketRpc` for React (connection lifecycle, `ready`, stable `sessionRef`). Optional peer `react` is documented for the React entry only.
