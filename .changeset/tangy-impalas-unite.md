---
"socka": minor
---

Add **`dispatchSockaInboundMessage`** (shared with **`attachSockaWebSocket`**), first-class **`socka/bun`** (`createSockaBunWebSocketHandlers`), **`socka/hono`** (`sockaHonoNodeWs` for `@hono/node-ws`), and **`socka/hono/cloudflare`** (`sockaHonoCloudflare` for `hono/cloudflare-workers`). Optional peers: **`@hono/node-ws`**, **`@hono/node-server`**, **`ws`**. README documents **`createData`**, session state / DO hibernation, and links to integration tests.
