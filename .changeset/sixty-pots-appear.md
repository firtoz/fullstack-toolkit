---
"@firtoz/router-toolkit": minor
---

Official React Router 8 support. Peer dependencies now accept `react-router@^7.14.2 || ^8.0.0` and `react@^19.2.7`. `formAction` handler args use RR8's `RouterContextProvider` for `context` (middleware always enabled in RR8). README updated for RR7/RR8.
