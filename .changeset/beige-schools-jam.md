---
"@firtoz/idb-collections": minor
---

New package for IndexedDB collection utilities. Provides `createKeyValCollection` and `keyvalCollectionOptions` for key-value adapter–backed TanStack DB collections, plus `tryExtractIndexedQuery` and `KeyRangeSpec` for IndexedDB query optimization.
