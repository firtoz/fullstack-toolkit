---
"@firtoz/chat-agent": patch
---

Add Bun unit tests for wire schemas (`sendMessage` validation, tool approval messages, `providerMetadata` on tool calls/deltas, and `defineTool` with `needsApproval`). Add `test` and `test:watch` scripts.
