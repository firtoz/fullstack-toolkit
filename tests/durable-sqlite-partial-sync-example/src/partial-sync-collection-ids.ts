/** WebSocket multiplex id for people room partial + mutation sync. */
export const PEOPLE_PARTIAL_SYNC_COLLECTION_ID = "people" as const;

/** WebSocket multiplex id for emoji grid partial + mutation sync. */
export const EMOJI_GRID_PARTIAL_SYNC_COLLECTION_ID = "emoji-grid" as const;
