{
	"name": "websocket-do-test",
	"version": "1.2.6",
	"private": true,
	"description": "Integration tests for @firtoz/websocket-do package using vitest-pool-workers",
	"type": "module",
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest watch",
		"typecheck": "tsc --noEmit -p ./tsconfig.json",
		"typegen": "bun run cf-typegen",
		"cf-typegen": "bun --cwd ../../packages/worker-helper cf-typegen $(pwd)",
		"lint": "biome check --write src",
		"lint:ci": "biome ci src",
		"format": "biome format src --write"
	},
	"dependencies": {
		"@firtoz/hono-fetcher": "^2.3.2",
		"@firtoz/websocket-do": "workspace:*",
		"hono": "catalog:",
		"zod": "catalog:",
		"msgpackr": "catalog:"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:",
		"wrangler": "catalog:"
	}
}
